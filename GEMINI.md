1. Do not use emojis in the responses, especially the code you're writing.
2. If the project is a Rust project, always run "cargo fmt" after writing code so that the structure is neatly formatted.
The project is a Rust workspace composed of three crates: `hdim-core` for core logic, `hdim-render` for image rendering, and `hdim-tui` for the text user interface.